# Godot 4+ specific ignores
.godot/
.nomedia

# Godot-specific ignores
.import/
export.cfg
export_credentials.cfg

# Imported translations (automatically generated from CSV files)
*.translation

# Mono-specific ignores
.mono/
data_*/
mono_crash.*.json

# Export output
build/

# Export presets can contain credentials for some platforms; this project has
# none, so the Web preset is tracked deliberately.
export_credentials.cfg
/tools
